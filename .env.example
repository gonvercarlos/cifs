# Database Configuration
DB_HOST=qadb.hallon.es
DB_PORT=5432
DB_NAME=eprensa
DB_USER=postgres
DB_PASS=your_password_here

# Base Path (optional, leave empty if running at root)
# Example: /cifs for running at http://domain.com/cifs
BASE_PATH=
